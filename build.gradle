ext {
    groovyVersion = '2.5.0-beta-3'
}

allprojects {
    apply plugin: 'groovy'
    apply plugin: 'idea'
    repositories {
        jcenter()
    }
    dependencies {
        compile "org.codehaus.groovy:groovy-all:$groovyVersion"
    }
}
